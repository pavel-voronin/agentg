{
  "name": "agentg",
  "description": "Claude Code channel bridge for AgentG Gateway. Streams Gateway events into Claude and exposes a minimal gateway-backed tool surface.",
  "version": "0.1.0",
  "keywords": ["agentg", "telegram", "channel", "mcp", "websocket"],
  "mcpServers": "./.mcp.json",
  "channels": [
    {
      "server": "agentg"
    }
  ]
}
