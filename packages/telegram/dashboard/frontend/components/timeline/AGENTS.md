# History Coverage Timeline Components

- This folder contains the restored Telegram history coverage timeline UI.
- Keep copied timeline UX behavior intact unless the user explicitly approves a redesign.
- Components must use `<script setup lang="ts">` and `<style scoped>`.
- Do not reintroduce target or template controls.
