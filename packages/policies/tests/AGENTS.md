# Policies Tests

- Tests here verify the generic policy endpoint package only.
- Keep module behavior out of this folder.
- Prefer file-store and endpoint composition tests over duplicating framework
  policy server tests.
