# Dashboard UI

- This folder owns small shared Dashboard UI primitives only.
- Do not add domain-specific UI, screen layouts, or business logic here.
- Components must use `<script setup lang="ts">` and `<style scoped>`.
