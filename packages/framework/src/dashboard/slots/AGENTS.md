# Dashboard Slots

- This folder owns generic slot contracts, resolution, runtime, and slot
  rendering components.
- Do not add domain slot names, domain layout decisions, or module-specific
  content here.
- Components must use `<script setup lang="ts">` and `<style scoped>`.
