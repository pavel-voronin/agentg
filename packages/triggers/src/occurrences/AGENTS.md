# Trigger Occurrences

- This folder owns occurrence data and state names.
- Occurrence keys must remain deterministic for `registrationKey + scheduledAt`.
- Keep action payload snapshots immutable after occurrence creation.
