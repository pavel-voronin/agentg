# Triggers Database

- This folder owns the Triggers Postgres schema and resource creation.
- Table names must use the `triggers_` prefix.
- Keep migrations package-local under `packages/triggers/drizzle`.
