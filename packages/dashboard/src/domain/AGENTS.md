# Dashboard Domain Logic

- This folder contains Dashboard-owned pure domain helpers.
- Do not import Vue components, server code, or domain module implementations.
- Keep helpers deterministic and side-effect free unless the file name makes the
  side effect explicit.
