# Runtime Rules

- Runtime code runs in the browser.
- Do not import Node-only modules.
- Provider content comes from `virtual:dashboard/providers`.
- Procedure calls go through the injected Dashboard host.
