apiVersion: agentg.dev/v1
kind: TelegramFileDownloadRule
metadata:
  name: requestedDocuments
  labels:
    subsystem: files
spec:
  causes:
    - explicit_request
  mediaKind: document
  maxBytes: null
