apiVersion: agentg.dev/v1
kind: TelegramFileDownloadRule
metadata:
  name: requestedThumbnails
  labels:
    subsystem: files
spec:
  causes:
    - explicit_request
  mediaKind: thumbnail
  maxBytes: null
